<!DOCTYPE html>
<html>
    <%- include('../partials/head', { hojasDeEstilo: ["productsList"] })%>
<body>
    <%- include('../partials/header') %>
    <main>
        <h3 class="tittle">Alimentos</h3>
        <section class="products">
            <% for(let i = 0; i < alimentos.length; i++){ %>
                <a class="control__a" href="/products/<%= alimentos[i].id %>">
                    <article>
                  <img src="/images/<%= alimentos[i].image %>">
                  <div class="products__details">
                      <p class="products__details-name">
                          <%= alimentos[i].name %> 
                      </p>
                      <p class="products__details-price">
                          <%= alimentos[i].price %>
                      </p>
                      <p>
                          <%= alimentos[i].description %>
                      </p>
                  </div>
                    </article>
                </a>
              <% } %>
        </section>
    </main>
    <%- include('../partials/footer') %>
</body>
</html>