const path = require("path");
const fs = require("fs");


const productsDbPath = path.join(__dirname, "../db/users.json");

const user = {
    login: function (req, res) {
        
    },
    register: function (req, res) {
        
    }
};

module.exports = user;