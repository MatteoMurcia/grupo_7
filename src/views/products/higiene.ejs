<!DOCTYPE html>
<html>
    <%- include('../partials/head', { hojasDeEstilo: ["productsList"] })%>
<body>
    <%- include('../partials/header') %>
    <main>
        <h3 class="tittle">Cuidado e higiene</h3>
        <section class="productsList">
            <% for(let i = 0; i < higiene.length; i++){ %>
                <a class="control__a" href="/products/<%= higiene[i].id %>">
                    <article>
                  <img src="/images/<%= higiene[i].image %>">
                  <div class="products__details">
                      <p class="products__details-name">
                          <%= higiene[i].name %> 
                      </p>
                      <p class="products__details-price">
                          <%= higiene[i].price %>
                      </p>
                      <p>
                          <%= higiene[i].description %>
                      </p>
                  </div>
                    </article>
                </a>
              <% } %>
        </section>
    </main>
    <%- include('../partials/footer') %>
</body>
</html>